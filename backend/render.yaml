services:
  - type: web
    name: videoconff-signaling
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheck:
      path: /api/health
    plan: free
