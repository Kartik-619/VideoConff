services:
  - type: web
    name: videoconff-signaling
    runtime: node
    buildCommand: npm install
    startCommand: node signaling/server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheck:
      path: /api/health
    plan: free
