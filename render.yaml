services:
  - type: web
    name: videoconff-signaling
    runtime: node
    rootDir: backend
    buildCommand: npm install && npx prisma generate
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheck:
      path: /api/health
    plan: free
