services:
  - type: web
    name: videoconff-signaling
    runtime: node
    buildCommand: cp package-backend.json package.json && npm install && npx prisma generate
    startCommand: node signaling/server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheck:
      path: /api/health
    plan: free
