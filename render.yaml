services:
  - type: web
    name: videoconff-signaling
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    envVars:
      - key: NODE_ENV
        value: production
    healthCheck:
      path: /api/health
    plan: free
